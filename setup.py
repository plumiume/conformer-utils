from setuptools import setup, find_packages

setup(
    name='conformer_utils',
    version='0.0.0',
    packages=[
        'conformer_utils'
    ],
    install_requires=[
        'torch',
        'torchaudio'
    ]
)